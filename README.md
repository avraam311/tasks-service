# Tasks Service

REST API сервис для управления задачами, написанный на языке Go с использованием принципов Clean Architecture.

## 📋 Описание

Tasks Service предоставляет API для создания, чтения, обновления и удаления задач (CRUD операции). Сервис построен с использованием модульной архитектуры, что обеспечивает легкость поддержки и расширения функциональности.

### Основные возможности

- ✅ Создание новых задач
- ✅ Получение всех задач
- ✅ Получение задачи по ID
- ✅ Обновление задач
- ✅ Удаление задач
- ✅ Валидация входных данных
- ✅ Структурированное логирование
- ✅ Graceful shutdown
- ✅ Docker контейнеризация

## 🛠 Технологический стек

- **Язык программирования:** Go 1.25.3
- **Архитектура:** Clean Architecture
- **Валидация:** Встроенная валидация Go структур
- **Логирование:** Structured logging (slog)
- **Контейнеризация:** Docker
- **Тестирование:** Go testing framework + Testify
- **Mock'и:** GoMock

## 🏗 Архитектура проекта

Проект построен по принципам Clean Architecture с разделением на слои:

```
├── cmd/                    # Точка входа в приложение
│   └── main.go
├── internal/              # Приватный код приложения
│   ├── api/              # API слой (HTTP handlers)
│   │   ├── handlers/     # Обработчики HTTP запросов
│   │   ├── middlewares/  # Middleware компоненты
│   │   ├── responses/    # Шаблоны ответов API
│   │   └── server/       # Конфигурация сервера
│   ├── service/          # Бизнес-логика
│   ├── repository/       # Работа с данными
│   ├── models/           # Модели данных
│   └── infra/           # Инфраструктурные компоненты
│       ├── config/       # Конфигурация
│       └── logger/       # Логирование
├── config/              # Конфигурационные файлы
├── Dockerfile           # Docker конфигурация
├── Makefile            # Сборка и утилиты
└── go.mod              # Зависимости Go
```

### Модели данных

**TaskDTO** - Data Transfer Object для входных данных:
```go
type TaskDTO struct {
    Header      string `json:"header" validate:"required"`
    Description string `json:"description"`
    Finished    bool   `json:"finished"`
}
```

**TaskDomain** - Доменная модель:
```go
type TaskDomain struct {
    ID          uint   `json:"id" validate:"required"`
    Header      string `json:"header" validate:"required"`
    Description string `json:"description" validate:"required"`
    Finished    bool   `json:"finished" validate:"required"`
}
```

## 🚀 Установка и запуск

### Требования

- Go 1.25.3 или выше
- Docker (опционально)
- golangci-lint для линтинга

### Локальная разработка

1. **Клонирование репозитория:**
```bash
git clone <repository-url>
cd tasks-service
```

2. **Установка зависимостей:**
```bash
go mod download
```

3. **Запуск сервера:**
```bash
go run cmd/main.go
```

Сервер запустится на порту 8080.

### Запуск через Docker

1. **Сборка образа:**
```bash
docker build -t tasks-service .
```

2. **Запуск контейнера:**
```bash
docker run -p 8080:8080 tasks-service
```

### Использование Makefile

Доступные команды:

```bash
make lint    # Запуск линтинга кода
make test    # Запуск тестов
make up      # Сборка и запуск через Docker
```

## 📡 API Документация

### Базовый URL
```
http://localhost:8080
```

### Эндпоинты

#### 1. Получение всех задач

**GET** `/todos`

**Ответ:**
```json
[
  {
    "id": 1,
    "header": "Заголовок задачи",
    "description": "Описание задачи",
    "finished": false
  }
]
```

#### 2. Получение задачи по ID

**GET** `/todos/{id}`

**Параметры:**
- `id` (uint) - ID задачи

**Ответ:**
```json
{
  "id": 1,
  "header": "Заголовок задачи",
  "description": "Описание задачи",
  "finished": false
}
```

**Ошибки:**
- `400 Bad Request` - Неверный ID задачи
- `400 Bad Request` - Задача не найдена

#### 3. Создание новой задачи

**POST** `/todos`

**Тело запроса:**
```json
{
  "header": "Новая задача",
  "description": "Описание новой задачи",
  "finished": false
}
```

**Ответ:**
```json
{
  "id": 1,
  "header": "Новая задача",
  "description": "Описание новой задачи",
  "finished": false
}
```

#### 4. Обновление задачи

**PUT** `/todos/{id}`

**Параметры:**
- `id` (uint) - ID задачи

**Тело запроса:**
```json
{
  "header": "Обновленная задача",
  "description": "Обновленное описание",
  "finished": true
}
```

**Ответ:**
```json
{
  "id": 1,
  "header": "Обновленная задача",
  "description": "Обновленное описание",
  "finished": true
}
```

#### 5. Удаление задачи

**DELETE** `/todos/{id}`

**Параметры:**
- `id` (uint) - ID задачи

**Ответ:** `204 No Content`

### Формат ответов

#### Успешный ответ (200 OK)
```json
{
  "data": { /* данные ответа */ }
}
```

#### Ошибка
```json
{
  "error": {
    "code": "код_ошибки",
    "message": "сообщение_об_ошибке"
  }
}
```

### Коды ошибок

- `ErrMethodNotAllowed` - Метод не разрешен
- `ErrInvalidID` - Неверный ID
- `ErrTaskNotFound` - Задача не найдена
- `ErrInternalServer` - Внутренняя ошибка сервера

## ⚙️ Конфигурация

### Файл конфигурации

Сервис использует JSON конфигурацию по пути `./config/local.json`:

```json
{
    "server": {
        "port": ":8080"
    }
}
```

### Параметры конфигурации

- `server.port` - Порт для запуска HTTP сервера (по умолчанию: `:8080`)

### Логирование

Сервис использует структурированное логирование с настройками:

- **Уровень:** info
- **Формат:** JSON
- **Выходной поток:** stdout

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
make test

# Или напрямую
go test ./...

# С покрытием кода
go test -cover ./...
```

### Структура тестов

- **API тесты:** `internal/api/handlers/tasks/handler_test.go`
- **Service тесты:** `internal/service/tasks/service_test.go`
- **Repository тесты:** `internal/repository/tasks/repository_test.go`

### Примеры использования API

#### Создание задачи
```bash
curl -X POST http://localhost:8080/todos \
  -H "Content-Type: application/json" \
  -d '{
    "header": "Изучить Go",
    "description": "Прочитать документацию по Go",
    "finished": false
  }'
```

#### Получение всех задач
```bash
curl http://localhost:8080/todos
```

#### Получение задачи по ID
```bash
curl http://localhost:8080/todos/1
```

#### Обновление задачи
```bash
curl -X PUT http://localhost:8080/todos/1 \
  -H "Content-Type: application/json" \
  -d '{
    "header": "Изучить Go (обновлено)",
    "description": "Прочитать документацию и написать примеры",
    "finished": true
  }'
```

#### Удаление задачи
```bash
curl -X DELETE http://localhost:8080/todos/1
```

## 🔧 Разработка

### Линтинг кода

Для проверки качества кода используется golangci-lint:

```bash
make lint
```

### Добавление новых зависимостей

```bash
go get <package-name>
go mod tidy
```

### Структура разработки

1. **Handlers** - Обрабатывают HTTP запросы, валидируют данные
2. **Services** - Содержат бизнес-логику
3. **Repository** - Управляют доступом к данным
4. **Models** - Определяют структуры данных

## 📦 Деплой

### Docker деплой

1. Соберите образ:
```bash
docker build -t tasks-service:latest .
```

2. Запустите контейнер:
```bash
docker run -d -p 8080:8080 --name tasks-service tasks-service:latest
```

### Конфиг

В текущей версии конфигурация осуществляется через JSON файл.